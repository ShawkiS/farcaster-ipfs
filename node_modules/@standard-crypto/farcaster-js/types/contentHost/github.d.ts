import { AddressActivity, Directory } from "../api";
import { ContentHost, SignedCast } from ".";
export declare class GithubGistContentHost implements ContentHost {
    private readonly _gist;
    private readonly _ready;
    static readonly DIRECTORY_FILENAME = "directory.json";
    static readonly ACTIVITY_FILENAME = "activity.json";
    constructor(personalAccessToken: string);
    directoryUrl(): Promise<string>;
    activityUrl(): Promise<string>;
    publishCast(cast: SignedCast): Promise<void>;
    updateDirectory(newDirectory: Directory): Promise<void>;
    getDirectory(): Promise<Directory | null>;
    bulkUpload(activity: AsyncIterable<AddressActivity>): Promise<void>;
    private _getOrCreateActivityFile;
}
